/**
 * Renders the Filters component.
 */
export function Filters() {
  return (
      <footer>
        <button>Clear All</button>
        <a href={'/'}>All</a>
        <a href={'/'}>Active</a>
        <a href={'/'}>Done</a>
      </footer>
  )
}
